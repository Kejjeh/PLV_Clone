name: PL ranking cache auto-pull (self-hosted)

# Auto-pulls Pitcher List rankings into data/research/pl_cache/ on a cadence-gated
# schedule, so the dashboards/triangulate read current PL ranks without a manual WebFetch.
# Runs in the evening AFTER PL publishes (articles land ~6-7 PM ET), so a same-evening run
# catches that day's edition. The puller (build_pl_cache.py) is cadence-gated and fail-soft
# — it only writes a cache once its new edition is actually out, and a short/garbled parse
# never overwrites a good cache.
#
# Coverage: all three via format-specific parsers — Top 100 SP (numbered list), closers
# (per-team role table → role + save% + synthesized rank), Top 150 hitters (position-tiered
# → appearance-order rank + position). Each is count-validated + fail-soft.
#
# Schedule: 01:00 UTC = 21:00 ET (EDT) / 20:00 ET (EST) — comfortably past the ~7 PM ET drop.
on:
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  pl-cache:
    runs-on: [self-hosted, Windows]
    timeout-minutes: 10
    defaults:
      run:
        shell: powershell
        working-directory: C:\Users\Joshua\plv_clone
    env:
      PYTHONUTF8: "1"
      PYTHONIOENCODING: "utf-8"
    steps:
      - name: Pull PL rankings (cadence-gated)
        run: python scripts/xfp/build_pl_cache.py

      - name: Commit & push refreshed PL cache
        run: |
          git add data/research/pl_cache
          git diff --cached --quiet
          if ($LASTEXITCODE -ne 0) {
            $stamp = Get-Date -Format 'yyyy-MM-dd'
            git commit -m "pl-cache: auto-pull rankings $stamp"
            git push origin main
          } else {
            Write-Host "No PL cache changes to commit."
          }
