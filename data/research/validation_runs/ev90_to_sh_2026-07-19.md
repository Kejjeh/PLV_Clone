---
signal: ev90_to_sh
formula: per (batter, year, split_day) — 90th percentile of launch_speed over the batter's balls in play (type=='X', launch_speed notna) with game_date <= season_start + split_day, shrunk k=40 BBE toward the as-of (year, split_day) population mean; zero-BBE rows take the population mean
outcome: ros_core_fp_per_pa (rh3 harness target, rest-of-season FP/PA after the split)
expected_sign: "+"
theory: top-of-distribution exit velocity stabilizes in ~40 BBE and reflects max power capability, potentially beyond the MEAN-contact features (hard_hit_pct, barrel_pct) already in RH3_FEATS
production_target: rh3
framing: in-season → ros (rh3 production framing, all split_days)
holdout_years: [2024, 2025]
training_years: [2018, 2019, 2021, 2022, 2023]
validation_script: scripts/xfp/validate_ev90_to_sh.py
date: 2026-07-19
verdict: MARGINAL
---

## Origin / why now

`recent_signal_tournament` (2026-07-18) flagged ev90_season +.083 (p=.068) and
hardhit_season +.081 as the only suggestive signals beyond the FP level, with a
"re-run ~Aug with more anchors" deferral. This run REPLACES the wait: the 2026
anchor count was the binding constraint only because that study was 2026-only —
the multi-year rolling substrate + rh3 harness support the full cross-year test
today (user request 2026-07-19: accelerate future-dated items via backtest).

## Rule 9 disposition of the sibling candidate

`hardhit_season` — HALTED at Step 2 (whiff_pct_trailing21 precedent):
`hard_hit_pct_to_sh` AND `barrel_pct_to_sh` are already in RH3_FEATS in the
same in-season framing. No candidate left as-specified. NOT run.
→ Bonferroni: this is a single-cell run (N=1).

## Rule 9 baseline

Full RH3_FEATS (all ~21 features incl. hard_hit_pct_to_sh, barrel_pct_to_sh,
xwoba_per_pa_to_sh) via `_validate_rh3_v3_helper.run_candidate_eval` — the same
harness as bip_to / sprint_speed_lag1. Candidate merged into the rolling frame
on (batter, year, split_day).

## Step 2.5 coverage

launch_speed exists 2015+; rolling grid 2018–2026 (2020 in substrate per its
rules). Same-year signal, no lookback → 5 training + 2 holdout years. BBE
volumes large; k=40 shrinkage removes thin-sample NaN. Clears Rule 5.

## Priors (pre-declared)

Expected REDUNDANT: ev90 correlates ~0.7–0.8 with hard_hit%/barrel% which are
already in the baseline. The tournament's +.083 was vs a NAIVE control (FP
level only). A fail here closes the ev90 deferral early; the August re-run
of the 2026 display leaderboard can still proceed for Rule-13 display ranking
but promotion would be off the table.
