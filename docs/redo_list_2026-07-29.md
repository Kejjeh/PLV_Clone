# Prior analyses to redo — FINAL (Phase E)

> Superseded by the prioritized table in
> `docs/next_level_program_2026-07-29.md` section 3, which is the canonical list.
> This file keeps the working notes and the per-entry reasoning that got trimmed
> for the report.

Assembled DURING execution. Each entry: what, why suspect, effort, priority.

## From W1 (stabilization wiring), 2026-07-29

### 1. Any `/fa-monitor` Signal-C HIGH alert acted on before today
- **Why suspect:** HIGH escalation fired at `pa_season >= 100` or on a 30-PA
  L21d hot-xwOBA read. Measured xwOBA crossing is **225 PA**, so HIGH could
  fire on samples 2–7x below decision-grade. Any pickup driven by a Signal-C
  HIGH may have been chasing noise.
- **Redo:** re-run `/fa-monitor`; compare the new HIGH set to the old. Names
  that drop out were never decision-grade.
- **Effort:** low (one run). **Priority:** MEDIUM — awareness-layer, and most
  Signal-C alerts route through a deep-dive before action.

### 2. Any `stuff_command_lens` STUFF-DECLINE / COMMAND-WATCH tag on an arm with
   < ~584 total 2026 pitches
- **Why suspect:** the old flat `len(d) >= 300` gate meant the *recent* window
  (last 30%) could be ~90 pitches against the 175 SwStr requires. `swstr_d` —
  the headline input to STUFF-DECLINE — was computable off a window carrying no
  forward information. Arms just over the old 300 threshold are the exposed set.
- **Redo:** re-run the lens across the roster + FA SP pool; list arms that now
  return None (previously tagged). Specifically re-check any *drop/sell* call
  justified by a STUFF-DECLINE tag on a low-pitch arm.
- **Effort:** low-medium. **Priority:** **HIGH** — this tag is explicitly a
  sell/structural-decline signal (CLAUDE.md gotcha #14), so a false one is
  expensive.
- **Known safe:** Framber (STUFF-DECLINE) and Soriano (COMMAND-WATCH), the two
  canonical cases, are both full-season arms and re-verified unchanged today.

### 3. Catcher-framing quintiles and any boom_stack tag that consumed them
- **Why suspect:** the framing pool filtered at 100 shadow pitches while the
  documented floor was 200. Catchers between 100–199 shadow pitches were
  receiving quintile assignments they should not have, shifting quintile
  boundaries for *everyone* and thus the 🧊 ELITE / ⚠ TAX framing tags.
- **Redo:** rebuild framing quintiles; diff tags vs previous. Any hitter/SP
  boom_stack component that keyed off a framing tag inherits the shift.
- **Effort:** low (rebuild + diff). **Priority:** LOW-MEDIUM — display tag,
  Rule-13 context-only, never moved a projection.

### 4. Short-window BB% / ISO / HR-rate reads in THIS WEEK's own hitter analyses
- **Why suspect:** the earlier FA/hitter work this session (before the cutoffs
  existed) presented L21d and 3-window BB% and power deltas as findings.
  BB% needs 175 PA, ISO 275 AB, HR-rate 275 PA — none reachable in a 21-day
  window; those specific claims are noise by construction.
- **Redo:** already partially done — the corrected FA board dropped in-season
  delta columns entirely. Remaining: the Bleday "walking more" observation
  (12.50 → 15.79 → 14.29 BB% across three windows) should be restated as
  *not measurable*, and his season-level 13.3% BB (which DOES clear 175 PA)
  is the only defensible version.
- **Effort:** trivial (restate in the final report). **Priority:** MEDIUM —
  it was a stated input to a recommendation Josh is acting on.

### 5. Any mid-season pitcher "command improved / getting more chases" read
- **Why suspect:** pitcher chase% and BB% NEVER stabilize in-window at any
  sample size. Any such claim in prior sessions is unsupportable, not merely
  low-confidence.
- **Redo:** no re-run possible — these reads should be retracted, not
  recomputed. Flag the pattern so it stops recurring (now gated in code).
- **Effort:** n/a. **Priority:** MEDIUM (process fix, already enforced).

### 6. `/sp-board` HR/9 structural lens — verify it is season-vs-career only
- **Why suspect:** contact-quality-AGAINST (hard-hit, barrel, HR-rate) never
  stabilizes in-window for SPs. The lens is documented as 2026-vs-career, which
  is legitimate; but if any code path computes it over a *window*, that path is
  invalid.
- **Redo:** audit the HR/9 lens implementation for window-scale computation.
- **Effort:** low (read one function). **Priority:** MEDIUM — not yet verified.

## Pending from later workstreams (placeholders)

- **W3b:** in-season bat-speed claims — specifically the Cam Smith read
  (+3.1 mph YoY, 98th pctl) and whether his *in-season* bat-speed trajectory
  supports or undercuts the "best longterm process play" verdict. Also whether
  the literature 30-swing threshold survives measurement.
- **W6:** if CRPS finds a band miscalibrated → every MC-derived output that
  consumed it (matchup-leverage P(win), sp_bench_mc, season_sim title odds,
  boom/bust tiers) needs re-derivation.
- **W7:** if LightGBM shows real nonlinear headroom → the "rate-model headroom
  is spent" premise behind prioritizing the decision layer would need revisiting.
- **W8:** any board built while a local `_norm` mis-keyed a join (the O'Hearn
  curly-apostrophe class) — codemod will surface which engines were affected.
- **Muncy collision window (2026-05-25 → 2026-07-29):** any FA board, roster
  audit, or league scan that touched a KNOWN_COLLISIONS name while the team-code
  drift was live could have carried a wrong-player projection. Highest-value
  check: boards that recommended or rejected Max Muncy, Luis García Jr., Will
  Smith, Jacob Wilson, or either Logan Allen.


---

## Resolved during the program (recorded so they are not re-investigated)

- **`/season-sim` title odds + its "+10% sigma" conclusion** — checked in code
  rather than assumed. `run_season_sim.py:303-305` never reads `proj['sigma2']`;
  it takes per-game hitter sigma from the player's own boxscore series or
  `fallback_sigma('H', default=3.2)`, both close to the measured 3.2502 truth.
  **NOT invalidated by the variance bug.**
- **The 2026-07-10 rp3 sigma coverage study** — reproduced digit-for-digit
  (n=868, cov50 44.9%, cov80 74.0%) and STRENGTHENED: CRPS puts the x2.41 band
  0.22% off optimum for the single-start frame. Its NO-CHANGE verdict stands.
- **The 60-cell inseason_delta_grid REJECTION** — two method defects were found
  in its harness (EARLIER leg was cumulative, not a window; two declared anchors
  silently ran on one snapshot each), but both bias toward FALSE POSITIVES, so the
  rejection is unaffected or strengthened. Corrections recorded in the memo; the
  harness must not be reused as-is.
- **Framber (STUFF-DECLINE) / Soriano (COMMAND-WATCH)** — the canonical
  stuff_command cases re-verified unchanged after the window-gate fix. Both are
  full-season arms, so the ~584-pitch requirement does not touch them.
- **No shipped rank or projection reverses.** Nothing was ranking off an
  in-season delta, a nonlinear learner, or the miscalibrated sigma — the sigma bug
  moved probabilities, not means or orders.
